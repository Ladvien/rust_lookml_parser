pub fn get_plural_keys() -> Vec<String> {
    vec![
        "dimension:",
        "measure:",
        "dimension_group:",
        "view:",
        "set:",
        "filter:",
        "filters:",
        "access_filter:",
        "bind_filters:",
        "map_layer:",
        "parameter:",
        "column:",
        "derived_column:",
        "include:",
        "explore:",
        "link:",
        "when:",
        "allowed_value:",
        "named_value_format:",
        "join:",
        "datagroup:",
        "access_grant:",
        "sql_step:",
        "action:",
        "param:",
        "form_param:",
        "option:",
        "user_attribute_param:",
        "assert:",
        "test:",
        "query:",
        "extends:",
        "aggregate_table:",
    ]
    .iter()
    .map(|f| f.to_string())
    .collect()
}
